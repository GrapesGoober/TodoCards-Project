# This is the script user.py, which will be handling all-things users
# This includes: authentication, changing usernames & password, 

# authenticates a user, returns True or False
def login(mydb, username, password):
    return True